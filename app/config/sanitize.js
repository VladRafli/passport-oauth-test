const sanitizeConfig = {
    /**
     * @type {import("sanitize-html").IOptions}
     */
    sanitizeAll: {
        allowedTags: [],
        allowedAttributes: {}
    }
}

export default sanitizeConfig